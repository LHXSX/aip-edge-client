name: '自动编译并部署到生产服务器'

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'src-tauri/**'

jobs:
  build-and-deploy:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            target: windows
          - platform: macos-latest
            target: macos
          - platform: ubuntu-latest
            target: linux

    runs-on: ${{ matrix.platform }}

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 安装Rust
        uses: dtolnay/rust-toolchain@stable

      - name: 安装Linux依赖
        if: matrix.platform == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev libappindicator3-dev librsvg2-dev patchelf

      - name: 安装前端依赖
        run: npm install

      - name: 编译Tauri应用
        run: npm run tauri build

      - name: 上传Windows产物
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe

      - name: 上传macOS产物
        if: matrix.platform == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-installer
          path: |
            src-tauri/target/release/bundle/dmg/*.dmg
            src-tauri/target/release/bundle/macos/*.app

      - name: 上传Linux产物
        if: matrix.platform == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: linux-installer
          path: |
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/appimage/*.AppImage

  deploy:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    if: success()
    
    steps:
      - name: 下载所有编译产物
        uses: actions/download-artifact@v4
        
      - name: 列出文件
        run: |
          echo "📦 编译产物："
          find . -type f \( -name "*.exe" -o -name "*.msi" -o -name "*.dmg" -o -name "*.AppImage" -o -name "*.deb" \)
      
      - name: 安装sshpass
        run: |
          sudo apt-get update
          sudo apt-get install -y sshpass
      
      - name: 上传到生产服务器
        env:
          SERVER_PASSWORD: ${{ secrets.SERVER_PASSWORD }}
        run: |
          echo "📤 上传Windows安装包..."
          sshpass -p "$SERVER_PASSWORD" scp -o StrictHostKeyChecking=no windows-installer/nsis/*.exe root@8.218.206.57:/opt/aip-ecosystem/downloads/AIP-Edge-Client-Setup-1.0.0.exe || true
          sshpass -p "$SERVER_PASSWORD" scp -o StrictHostKeyChecking=no windows-installer/msi/*.msi root@8.218.206.57:/opt/aip-ecosystem/downloads/AIP-Edge-Client-1.0.0.msi || true
          
          echo "📤 上传macOS安装包..."
          sshpass -p "$SERVER_PASSWORD" scp -o StrictHostKeyChecking=no macos-installer/dmg/*.dmg root@8.218.206.57:/opt/aip-ecosystem/downloads/AIP-Edge-Client-1.0.0.dmg || true
          
          echo "📤 上传Linux安装包..."
          sshpass -p "$SERVER_PASSWORD" scp -o StrictHostKeyChecking=no linux-installer/appimage/*.AppImage root@8.218.206.57:/opt/aip-ecosystem/downloads/AIP-Edge-Client-1.0.0.AppImage || true
          sshpass -p "$SERVER_PASSWORD" scp -o StrictHostKeyChecking=no linux-installer/deb/*.deb root@8.218.206.57:/opt/aip-ecosystem/downloads/AIP-Edge-Client-1.0.0.deb || true
          
          echo "✅ 所有文件上传完成"
          
      - name: 设置服务器文件权限
        env:
          SERVER_PASSWORD: ${{ secrets.SERVER_PASSWORD }}
        run: |
          sshpass -p "$SERVER_PASSWORD" ssh -o StrictHostKeyChecking=no root@8.218.206.57 "chmod 644 /opt/aip-ecosystem/downloads/*.exe /opt/aip-ecosystem/downloads/*.msi /opt/aip-ecosystem/downloads/*.dmg /opt/aip-ecosystem/downloads/*.AppImage /opt/aip-ecosystem/downloads/*.deb 2>/dev/null; ls -lh /opt/aip-ecosystem/downloads/ | grep AIP"

